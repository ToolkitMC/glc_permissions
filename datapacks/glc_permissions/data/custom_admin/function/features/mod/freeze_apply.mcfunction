# ═══════════════════════════════════════════════════
# Freeze Apply (v1.1.0) - called as target player
# Macro: {action:"freeze|unfreeze"}
# "if string" kaldırıldı → ayrı fonksiyona yönlendir
# ═══════════════════════════════════════════════════
$function custom_admin:features/mod/freeze_$(action)
